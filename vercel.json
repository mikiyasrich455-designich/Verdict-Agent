{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "env": {
    "VERCEL": "true"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/proxy/$1"
    }
  ]
}
